apiversion: V1
kind: ReplecationController
metadata:
  name:myonlineapp-rc
spec:
  replicas: 3
  selector:
    app: myonlineapp
    version: 2.6.1
  template:
    metaddata:
      labels:
        app:myonlineapp
        version:2.6.1
      spec:
         containers:
         - name: myonllineapp-container
           image: husenpatel/dockerrepo
           ports: 
           - containerPort: 8080
